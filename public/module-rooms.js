document.addEventListener('DOMContentLoaded',()=>{const form=document.createElement('div');form.className='room-tools';form.innerHTML='<input id="safeRoomSearch" placeholder="Oda ara…" aria-label="Oda ara">';const list=document.querySelector('#rooms');document.querySelector('#createRoom')?.after(form);const filter=()=>{const q=form.querySelector('input').value.toLocaleLowerCase('tr-TR');list?.querySelectorAll('.roomcard').forEach(card=>card.hidden=!card.textContent.toLocaleLowerCase('tr-TR').includes(q))};form.querySelector('input').addEventListener('input',filter)});
