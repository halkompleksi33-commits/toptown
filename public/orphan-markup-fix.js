(()=>{const clean=()=>{const walk=document.createTreeWalker(document.body,NodeFilter.SHOW_TEXT);const nodes=[];while(walk.nextNode())if(/tton id="adminButton" hidden>Yönetici paneli/.test(walk.currentNode.nodeValue||''))nodes.push(walk.currentNode);nodes.forEach(n=>n.remove())};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',clean);else clean();setTimeout(clean,800)})();
