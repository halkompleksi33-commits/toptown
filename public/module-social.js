document.addEventListener('DOMContentLoaded',()=>{const b=document.createElement('button');b.textContent='👥 Sosyal';const d=document.createElement('dialog');d.innerHTML='<form method="dialog"><button class="close">×</button></form><h2>Sosyal</h2><div id="socialData">Yükleniyor…</div>';document.body.append(d);b.onclick=async()=>{d.showModal();const token=sessionStorage.getItem('toptown-session')||'';try{const r=await fetch('/api/friends',{headers:{Authorization:'Bearer '+token}}),rows=await r.json();d.querySelector('#socialData').textContent=Array.isArray(rows)&&rows.length?rows.map(x=>(x.emoji||'🙂')+' '+x.name).join(' · '):'Henüz arkadaşın yok.'}catch{d.querySelector('#socialData').textContent='Sosyal veriler yüklenemedi.'}};document.querySelector('header')?.append(b)});
