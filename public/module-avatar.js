document.addEventListener('DOMContentLoaded',()=>{const form=document.querySelector('#profileForm');if(!form)return;const input=document.createElement('input');input.type='file';input.accept='image/png,image/jpeg,image/webp';const b=document.createElement('button');b.type='button';b.textContent='🖼️ Gözat';b.onclick=()=>input.click();input.onchange=()=>{const f=input.files?.[0];if(!f)return;if(f.size>150000){alert('Görsel en fazla 150 KB olmalı.');return}const r=new FileReader();r.onload=async()=>{const t=sessionStorage.getItem('toptown-session')||'',emoji=document.querySelector('#emoji')?.value||'🙂';const x=await fetch('/api/profile',{method:'POST',headers:{'Content-Type':'application/json',Authorization:'Bearer '+t},body:JSON.stringify({emoji,avatar:r.result})});if(!x.ok)alert('Görsel kaydedilemedi.');else alert('Profil görseli kaydedildi.')};r.readAsDataURL(f)};form.append(input,b)});
