document.addEventListener('DOMContentLoaded',()=>{const b=document.createElement('button');b.textContent='🔔';b.title='Bildirimler';const d=document.createElement('dialog');d.innerHTML='<form method="dialog"><button class="close">×</button></form><h2>Bildirimler</h2><div id="noticeData">Yükleniyor…</div>';document.body.append(d);b.onclick=async()=>{d.showModal();try{const r=await fetch('/api/notifications',{headers:{Authorization:'Bearer '+(sessionStorage.getItem('toptown-session')||'')}}),x=await r.json();d.querySelector('#noticeData').textContent=x.items?.length?x.items.map(i=>'• '+i.text).join('\n'):'Yeni bildirimin yok.'}catch{d.querySelector('#noticeData').textContent='Bildirimler yüklenemedi.'}};document.querySelector('header')?.append(b)});
